# events { worker_connections 1024; }

# http {
#     upstream my_app {
#         # if the clones aren't ready immediately
#         # use 'max_fails' and 'fail_timeout' to give some time and breathing room
#         server app-clone-1:8000 max_fails=3 fail_timeout=5s;
#         server app-clone-2:8000 max_fails=3 fail_timeout=5s;
#     }

#     server {
#         listen 80;
#         location / {
#             proxy_pass http://my_app;
#             proxy_set_header Host $host;
#             proxy_set_header X-Real-IP $remote_addr;
#         }
#     }
# }

events { worker_connections 1024; }

http {
    upstream fastapi_cluster {
        # Use the service names from docker-compose.yml
        server app-clone-1:8000;
        server app-clone-2:8000;
    }

    server {
        listen 80;

        location / {
            proxy_pass http://fastapi_cluster;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            
            # Help Nginx recover if a clone is temporarily down
            proxy_next_upstream error timeout invalid_header http_500 http_502 http_503 http_504;
        }
    }
}